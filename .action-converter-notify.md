*5 Actions — 2026-06-12*
Shape: Bootstrap mode: configure notifications, enable daily skills, run first digest, seed soul

1. Add TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID secrets to repo Settings → Secrets & Variables → Actions
why: Every ./notify call is silently skipped — zero channels configured; the system can't reach you
done: ./notify "test" produces a Telegram message in the configured chat
loop: notification-channels

2. Set enabled: true for priority-brief, hn-digest, and narrative-tracker in aeon.yml
why: Three high-signal daily skills are installed but disabled; one file edit unlocks daily autonomous output
done: aeon.yml shows enabled: true for all three; change committed to main
loop: enable-daily-skills

3. Trigger the digest skill via `gh workflow run aeon.yml -f skill=digest`
why: "Run first digest" remains in Next Priorities since day one; one command produces the baseline output
done: gh run list shows a completed digest run with status success
loop: run-first-digest

4. Populate soul/SOUL.md with Identity, Worldview, and Interests sections (≥3 bullets each)
why: write-tweet ran today without a soul file — voice is generic; all future content inherits this file
done: soul/SOUL.md has non-empty Identity, Worldview, Interests sections committed to main
loop: soul-config

5. Rewrite memory/MEMORY.md Next Priorities to replace the two template lines with actual goals
why: Template Next Priorities will keep triggering BOOTSTRAP mode on every action-converter run until cleared
done: MEMORY.md no longer contains "Configure notification channels" or "Run first digest" lines
loop: memory-populate

sources: memory=26 logs=1 topics=4 prs=0 cron_failing=0 mode=BOOTSTRAP
